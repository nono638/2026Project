# Task 040: User Documentation — README Updates + Experiment Guide

## What to Build

Update the README to cover features added since it was written (rerankers, cost guard,
experiment scripts, constraint analysis), and add a `docs/` directory with two guides:
output format reference and experiment running guide.

This is the MVP-facing documentation for the April 7th demo.

## Files to Modify

### 1. `README.md`

**Add these sections/updates (preserve everything that already exists):**

**a) Rerankers section** — Add after the "Retrieval Modes" table, before "CLI Reference":

```markdown
### Rerankers

Optional cross-encoder reranking between retrieval and generation.

| Reranker | Flag | Model | Size |
|----------|------|-------|------|
| MiniLM | `--reranker minilm` | ms-marco-MiniLM-L-6-v2 | 22M params |
| BGE | `--reranker bge` | BAAI/bge-reranker-v2-m3 | 278M params |
| None | `--reranker none` (default) | No reranking | — |
```

**b) CLI Reference updates** — Add these missing flags to the CLI Reference section:

```
  --reranker NAME          Reranker: minilm, bge, or none (default: none)
  --reranker-top-k N       Chunks to keep after reranking (default: 3)
  --retrieval-top-k N      Chunks to retrieve before reranking (default: 5)
  --ollama-host URL        Remote Ollama server URL
  --max-cost USD           Maximum API spend ceiling (default: $10)
  --resume                 Resume interrupted experiment (experiment scripts only)
```

**c) Experiment Scripts section** — Add after CLI Reference, before "Implementing Your
Own Components":

```markdown
## Running the Research Experiments

RAGBench includes three pre-configured experiments. Each has its own script with
checkpoint/resume support.

| Experiment | Script | Matrix | Purpose |
|------------|--------|--------|---------|
| Exp 0: Scorer Validation | `run_experiment_0.py` | 50 queries × 6 judges | Validate LLM-as-judge reliability |
| Exp 1: Strategy × Model | `run_experiment_1.py` | 5 strategies × 6 models | Does strategy compensate for model size? |
| Exp 2: Chunking × Model | `run_experiment_2.py` | 4 chunkers × 4 models | How much does chunking matter? |

See [docs/running-experiments.md](docs/running-experiments.md) for detailed instructions.
```

**d) Output Format section** — Add after "Running the Research Experiments":

```markdown
## Output Format

Experiment results are saved to `results/experiment_N/raw_scores.csv`. See
[docs/output-format.md](docs/output-format.md) for a complete column reference.
```

**e) Update Project Structure** — Add these entries:

```
  rerankers/            # Cross-encoder reranker implementations (MiniLM, BGE)
  cost_guard.py         # API spend tracking with configurable ceiling
  features.py           # Meta-learner feature extraction
  train.py              # Meta-learner training (XGBoost)
  predict.py            # Meta-learner prediction
  ...
scripts/
  run_experiment_0.py   # Scorer validation experiment
  run_experiment_1.py   # Strategy x model size experiment
  run_experiment_2.py   # Chunking x model size experiment
  experiment_utils.py   # Shared experiment infrastructure
  generate_gallery.py   # Findings gallery site generator
  generate_experiment0_dashboard.py  # Exp 0 interactive dashboard
  ...
docs/
  output-format.md      # CSV column reference
  running-experiments.md # How to run experiments (local + RunPod)
```

**f) Update test count** — Change `369 tests` to `527 tests`.

## Files to Create

### 2. `docs/output-format.md`

Column reference for all experiment CSVs. Organized by category.

**Content:**

```markdown
# Output Format Reference

All experiments produce a `raw_scores.csv` in their output directory. This document
describes every column.

## Common Columns (All Experiments)

### Query & Answer
| Column | Type | Description |
|--------|------|-------------|
| `question` | str | The query text sent to the RAG pipeline |
| `gold_answer` | str | Reference answer from the dataset (empty if no gold data) |
| `rag_answer` | str | Answer generated by the RAG pipeline |

### Extrinsic Metrics (Gold Comparison)
Only populated when gold answers exist (HotpotQA, SQuAD).

| Column | Type | Range | Description |
|--------|------|-------|-------------|
| `gold_exact_match` | bool | T/F | Does the RAG answer contain the gold answer? (case-insensitive) |
| `gold_f1` | float | 0-1 | Word-level F1 between RAG answer and gold answer |
| `gold_bertscore` | float | 0-1 | BERTScore F1 — semantic similarity via contextual embeddings |

### Intrinsic Metrics (LLM-as-Judge)
Scored by the configured LLM judge (default: Gemini 2.5 Flash).

| Column | Type | Range | Description |
|--------|------|-------|-------------|
| `faithfulness` | float | 0-1 | Does the answer stick to the retrieved context? |
| `relevance` | float | 0-1 | Does the answer address the question? |
| `conciseness` | float | 0-1 | Is the answer appropriately concise? |
| `quality` | float | 0-1 | Mean of faithfulness, relevance, and conciseness |

### Latency
| Column | Type | Unit | Description |
|--------|------|------|-------------|
| `strategy_latency_ms` | float | ms | Time for the RAG strategy to generate an answer |
| `scorer_latency_ms` | float | ms | Time for the LLM judge to score the answer |
| `total_latency_ms` | float | ms | Sum of strategy + scorer latency |

### Pipeline Metadata
| Column | Type | Description |
|--------|------|-------------|
| `chunk_type` | str | Chunker used (recursive, fixed, sentence, semantic) |
| `chunk_size` | int | Target chunk size in characters |
| `chunk_overlap` | int | Character overlap between chunks |
| `num_chunks` | int | Number of chunks created from the document |
| `embed_provider` | str | Embedding provider (ollama, huggingface, google) |
| `embed_model` | str | Embedding model name |
| `embed_dimension` | int | Embedding vector dimension |
| `retrieval_mode` | str | Retrieval mode (hybrid, dense, sparse) |
| `retrieval_top_k` | int | Number of chunks retrieved |
| `num_chunks_retrieved` | int | Actual chunks returned by retriever |
| `context_char_length` | int | Total character length of retrieved context |
| `reranker_model` | str/null | Reranker model name, or null if none |
| `reranker_top_k` | int/null | Chunks kept after reranking, or null |
| `llm_provider` | str | LLM provider (ollama) |
| `llm_host` | str | Ollama server URL or "local" |
| `llm_model` | str | Ollama model name (e.g., qwen3:4b) |
| `dataset_name` | str | Dataset used (hotpotqa, squad, custom) |
| `dataset_sample_seed` | int | Random seed for reproducibility |

## Experiment-Specific Columns

### Experiment 0 (Scorer Validation)
| Column | Type | Description |
|--------|------|-------------|
| `example_id` | str | Unique identifier for each query |
| `{judge_name}_faithfulness` | float | Per-judge faithfulness score |
| `{judge_name}_relevance` | float | Per-judge relevance score |
| `{judge_name}_conciseness` | float | Per-judge conciseness score |
| `{judge_name}_quality` | float | Per-judge mean quality |

Judge name prefixes: `google_gemini_2_5_flash_lite`, `google_gemini_2_5_flash`,
`google_gemini_2_5_pro`, `anthropic_claude_haiku_4_5_20251001`,
`anthropic_claude_sonnet_4_20250514`.

### Experiment 1 (Strategy x Model)
| Column | Type | Description |
|--------|------|-------------|
| `strategy` | str | RAG strategy (naive, self_rag, multi_query, corrective, adaptive) |
| `model` | str | LLM model (qwen3:0.6b, qwen3:1.7b, qwen3:4b, qwen3:8b, gemma3:1b, gemma3:4b) |

### Experiment 2 (Chunking x Model)
| Column | Type | Description |
|--------|------|-------------|
| `chunker` | str | Chunking strategy (recursive, fixed, sentence, semantic) |
| `model` | str | LLM model (qwen3:0.6b, qwen3:1.7b, qwen3:4b, qwen3:8b) |
```

### 3. `docs/running-experiments.md`

Step-by-step guide for running each experiment.

**Content structure:**

```markdown
# Running Experiments

## Prerequisites

- Ollama running locally or on a remote server (RunPod)
- API key for scoring in `.env` (GEMINI_API_KEY for default scorer)
- Required models pulled (`python scripts/pull_models.py`)

## Experiment 0: Scorer Validation

**Purpose:** Validate that LLM-as-judge scoring is reliable before trusting it on
thousands of runs.

**What it does:** Takes 50 HotpotQA examples, generates answers with Qwen3-4B + NaiveRAG,
then scores each answer with multiple LLM judges. Compares judge scores against gold
metrics (BERTScore, F1) and inter-judge agreement.

**Run:**
```bash
python scripts/run_experiment_0.py
```

**Options:**
- `--n 50` — Number of HotpotQA examples (default: 50)
- `--skip-generation` — Re-score existing answers without regenerating
- `--judges flash,haiku` — Run specific judges only (substring match)
- `--ollama-host URL` — Remote Ollama server
- `--max-cost 5.0` — API spend ceiling in USD

**Output:** `results/experiment_0/raw_scores.csv`, `results/experiment_0/report.md`

**Expected duration:** ~30 min for generation, ~5 min per judge for scoring.

## Experiment 1: Strategy x Model Size

**Purpose:** Test whether a smart RAG strategy on a small model can outperform a naive
strategy on a large model.

**Matrix:** 5 strategies x 6 models = 30 configurations, each on 200 HotpotQA examples
= 6,000 total runs.

**Held constant:** RecursiveChunker(500, 100), mxbai-embed-large, hybrid retrieval.

**Run:**
```bash
# Full run (several hours on GPU)
python scripts/run_experiment_1.py --ollama-host http://YOUR_RUNPOD_IP:11434

# Resume after interruption
python scripts/run_experiment_1.py --resume --ollama-host http://YOUR_RUNPOD_IP:11434

# Test with subset
python scripts/run_experiment_1.py --models qwen3:4b --strategies naive,self_rag
```

**Options:**
- `--models MODEL[,MODEL]` — Subset of models to run
- `--strategies STR[,STR]` — Subset of strategies to run
- `--resume` — Skip configs already in raw_scores.csv
- `--skip-generation` — Re-score existing answers
- `--scorer provider:model` — Scorer (default: google:gemini-2.5-flash)
- `--ollama-host URL` — Remote Ollama server
- `--max-cost 10.0` — API spend ceiling

**Output:** `results/experiment_1/raw_scores.csv`, `results/experiment_1/report.md`

**Expected duration:** ~6-8 hours on RunPod A5000 for full matrix.

## Experiment 2: Chunking x Model Size

**Purpose:** Measure how much chunking strategy affects RAG quality across model sizes.

**Matrix:** 4 chunkers x 4 Qwen3 models = 16 configurations, each on 200 HotpotQA
examples = 3,200 total runs.

**Held constant:** NaiveRAG strategy, mxbai-embed-large, hybrid retrieval.

**Run:**
```bash
python scripts/run_experiment_2.py --ollama-host http://YOUR_RUNPOD_IP:11434
python scripts/run_experiment_2.py --resume --ollama-host http://YOUR_RUNPOD_IP:11434
```

**Options:** Same as Experiment 1 but `--chunkers` instead of `--strategies`.

**Output:** `results/experiment_2/raw_scores.csv`, `results/experiment_2/report.md`

## Running on RunPod

For experiments that need a GPU:

1. Create a pod: `python deploy/setup_pod.py`
2. Note the pod IP from the output
3. Run experiments with `--ollama-host http://POD_IP:11434`
4. When done, terminate the pod from the RunPod dashboard

The pod runs Ollama and automatically pulls required models during setup.

See `deploy/SETUP_GUIDE.md` for detailed RunPod instructions.

## Generating Visualizations

After experiments complete:

```bash
# Experiment 0 dashboard
python scripts/generate_experiment0_dashboard.py

# Full findings gallery (all experiments)
python scripts/generate_gallery.py
```

Output goes to `visuals/` and `site/`.

## Cost Estimates

| Experiment | Generation Cost | Scoring Cost | Total |
|------------|----------------|--------------|-------|
| Exp 0 (50 queries, 1 config) | ~$0 (local Ollama) | ~$0.05 (6 judges) | ~$0.05 |
| Exp 1 (200 queries, 30 configs) | ~$0 (local Ollama) | ~$0.60 (Flash) | ~$0.60 |
| Exp 2 (200 queries, 16 configs) | ~$0 (local Ollama) | ~$0.32 (Flash) | ~$0.32 |
| RunPod GPU (A5000) | $0.27/hr | — | ~$2-3 per experiment |

All scoring uses Gemini 2.5 Flash by default (free tier covers small experiments).

## Troubleshooting

### "Cannot connect to Ollama"
- Local: Make sure Ollama is running (`ollama serve`)
- Remote: Check the --ollama-host URL includes the port (`:11434`)
- RunPod: Verify the pod is running and port 11434 is exposed

### "COST LIMIT REACHED"
The `--max-cost` ceiling was hit. Results are saved up to that point. Increase the
ceiling or use `--skip-generation` to re-score existing answers only.

### "Model not found"
Run `python scripts/pull_models.py` or `ollama pull MODEL_NAME` to download the model.

### Tests failing with import errors
Activate the virtual environment: `.venv\Scripts\activate` (Windows) or
`source .venv/bin/activate` (Unix). Then `pip install -r requirements.txt`.
```

## What NOT to Touch

- Do NOT modify any Python source code — this is docs only
- Do NOT modify ENVIRONMENT.md (it has its own changelog format)
- Do NOT modify CLAUDE.md or .claude/ files
- Do NOT create a docs site generator (Sphinx/MkDocs) — plain markdown is fine for MVP

## Testing

No automated tests for documentation. But verify:
- All file paths mentioned in docs actually exist
- All CLI flags mentioned match the argparse definitions in scripts
- All column names match what the experiment scripts produce
- Links between README and docs/ files are correct

See pre-written tests in the tests/ directory of this spec.
